

% %dbsIDs = {'DBS3002', 'DBS3004', 'DBS3006', 'DBS3010', 'DBS3011', 'DBS3014', ...
%           'DBS3015', 'DBS3016', 'DBS3017', 'DBS3018', 'DBS3019', 'DBS3020', ...
%           'DBS3032', 'DBS4079', 'DBS4080'};

dbsID = 'DBS3004';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{2,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);
%%
dbsID = 'DBS3006';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{3,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);
%%
dbsID = 'DBS3010';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{4,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);

%%
dbsID = 'DBS3011';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{5,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);

%%
dbsID = 'DBS3014';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{6,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);
%%
dbsID = 'DBS3015';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{7,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);
%%
dbsID = 'DBS3016';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{8,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);
%%
dbsID = 'DBS3017';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{9,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);
%%
dbsID = 'DBS3018';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{10,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);
%%
dbsID = 'DBS3019';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{11,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);
%%
dbsID = 'DBS3020';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{12,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);
%%
dbsID = 'DBS3032';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{13,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);
%%
dbsID = 'DBS4079';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{14,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);
%%
dbsID = 'DB4080';

% Assuming subtable.phoneme_tables{5,1} is the table you want to export
phonemeTable = subtable.phoneme_tables{15,1};

% Define the path and filename for the Excel file
excelFilePath = 'C:\Users\amsmeier\dbs_triplet\daphne_scripts\DataForDTToLookAt.xlsx';  % Update the path as needed

% Write the table to an Excel file
writetable(phonemeTable, excelFilePath, 'Sheet', dbsID);

%%

% Load each triplet table into a separate variable and display it
for idx = 1:15
    variableName = sprintf('tripletTable%d', idx);  % Create a unique variable name for each table
    eval([variableName ' = subtable.triplet_tables{' num2str(idx) ',1};']);  % Assign the table to the variable
end

% Display all tables to confirm they are loaded
disp('All triplet tables are loaded into separate variables in the workspace.');
